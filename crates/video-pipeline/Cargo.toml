[package]
name = "video-pipeline"
version = "0.1.0"
edition = "2021"

[dependencies]
tracing = { workspace = true }

[target.'cfg(target_os = "macos")'.dependencies]
# No external crates needed — we use raw FFI to Apple frameworks
